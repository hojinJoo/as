# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /data: libri2mix.yaml
  - override /model: audioslot.yaml
  - override /callbacks: audioslot.yaml
  - override /trainer: ddp_spawn.yaml
  # - override /trainer: test.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["audio_slot", "libri2mix"]


 

seed: null

trainer:
  min_epochs: 10
  max_epochs: 378
  gradient_clip_val: 0.5
  check_val_every_n_epoch: 3
model:
  optimizer:
    lr: 0.0
  # net:
  #   lin1_size: 128
  #   lin2_size: 256
  #   lin3_size: 64

train : True

data:
  #r1 : 36
  batch_size: 12

logger:
  wandb:
    tags: ${tags}
    group: "audio_slot"

hydra :
  job_logging :
    handlers:
      file:
        filename: ${paths.output_dir}/${hydra.job.name}.log

ckpt_path : /workspace/as/epoch=085_train_loss=0.0275.ckpt